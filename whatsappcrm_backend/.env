# whatsappcrm_backend/.env

# --- Database Settings for Django ---
DB_ENGINE=django.db.backends.postgresql   # Matches your settings.py default
DB_NAME=whatsapp_crm_dev                  # MUST MATCH POSTGRES_DB in the 'db' service environment
DB_USER=crm_user                          # MUST MATCH POSTGRES_USER in the 'db' service environment
DB_PASSWORD=kayden  # MUST MATCH POSTGRES_PASSWORD in the 'db' service environment
#DB_HOST=db                                # CRITICAL: This is the service name of your PostgreSQL container in docker-compose.yml
DB_PORT=5432                              # Standard PostgreSQL port

# ... other environment variables ...